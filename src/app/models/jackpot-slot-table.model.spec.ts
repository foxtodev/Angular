import { JackpotSlotTable } from './jackpot-slot-table.model';

describe('JackpotSlotTable', () => {
  it('should create an instance', () => {
    expect(new JackpotSlotTable()).toBeTruthy();
  });
});
